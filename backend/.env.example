# Database Configuration
# Required. Uses postgresql+psycopg dialect (SQLAlchemy 2.x + psycopg v3)
DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/esp_db

# Application Settings (Placeholder)
# APP_ENV=
# SECRET_KEY=
